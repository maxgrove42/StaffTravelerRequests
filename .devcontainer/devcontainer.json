{
    "name": "StaffTravelerRequests",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "settings": {
        
    },
    "extensions": [
        
    ],
    "postCreateCommand": "apt-get update && apt-get install -y git",
    "forwardPorts": [
        
    ]
}
